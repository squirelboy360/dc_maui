name: app_module
description: "A new Flutter module project."

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter

  # cupertino_icons: ^1.0.8

dev_dependencies:
  # flutter_test:
  #   sdk: flutter
  # flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

flutter:
  module:
    androidX: true
    androidPackage: com.example.app_module
    iosBundleIdentifier: com.example.appModule
